@page
@model CryptidProject.Pages.Team.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>BigFoot Database!</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Cryptids[0].CryptidName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cryptids[0].OtherName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cryptids[0].Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cryptids[0].Location)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Cryptids) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CryptidName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OtherName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Location)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a> |
                <a asp-page="./Bigfoot" asp-route-id="@item.ID">Bigfoot</a> |
                    <a asp-page="./UnitedStates" asp-route-id="@item.ID">MAP</a> 
            </td>
        </tr>
}
    </tbody>
</table>
